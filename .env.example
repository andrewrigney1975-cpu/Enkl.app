DB_PASSWORD=change-me
JWT_SIGNING_KEY=change-me-to-a-random-32-plus-character-string
ASPNETCORE_ENVIRONMENT=Production

# v4 Phase 1 AI Assistant — optional. Leave unset to disable the feature; the endpoint throws a
# clear error until it's set. Never commit a real key here.
ANTHROPIC_API_KEY=
